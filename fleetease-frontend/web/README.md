React Web UI for Fleetease (skeleton)
Run:
cd web
npm install
npm start

Notes:
- Set REACT_APP_API_URL to backend API base (e.g., http://localhost:5000/api)
- Set REACT_APP_SOCKET_URL to socket server (e.g., http://localhost:5000)